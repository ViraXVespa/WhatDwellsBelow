# Park: Grok Bot setup and leftover agent jobs

Status: parked
Read when: resume parked agent_setup, GROK-BOT.md, Bot refresh, ritual tools

Not a boot file.

## Still open after relic-lang

1. Rewrite `GROK-BOT.md` to the product model in official docs.x.ai/grok-bot: short profile (job + boot + disk/branch/PR + off-limits), standing rules in `BOT.md`, first message names the clone branch. Skills are an account-wide private library, not repo `.grok/skills/`. Routines only after a skill. Auto-review Allow: git add/commit/push on `bot/*` under `/workspace/WhatDwellsBelow`. Ask first: push `main`, `gh pr merge`, force-push. Execution on Local Computer = Never (desktop only; does not block the VM).
2. Confirm every `design/grok-bot-*.md` Verify block is `Prove per BOT.md` with no Steam path, `WDB_ROOT`, or pc-offload runners.
3. Decide fate of `tools/sync_agent_skills.ps1` and `tools/week_start.ps1` (files still exist; agents must not run them).
4. `design/load-graph.md` Increment diary if it survived the slim.
5. `design/pc-offload.md` prose that still names Bot.

Do not open Bot Job files from a web park unless the User names that job.
