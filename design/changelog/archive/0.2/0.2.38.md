## 0.2.38
- Great Axe slam and staff special hit the occupancy disk at slam / bolt radius again (no more 8-pixel screen slop)
- Dummy / enemy glance flag uses a property check, so slam on the Placeholdia dummy no longer errors
- Touch walk stick stays hidden until a directional drag; a parked left finger does not cancel pinch-zoom
- Touch cluster sits in the right half: small map / food / potion / pause row over a large attack / special / interact / dash pad
- World camera zoom max is 4.0 (default still 1.75)

Summary: Circle specials connect again, phone pinch-zoom survives a resting left thumb, and the touch pad is larger and more see-through.

## Agent
- `scripts/combat/cover.gd` — `_disk_hits_sprite` projects the world disk
- `scripts/combat/player_hit.gd` — `"last_glance" in e`
- `scripts/ui/touch_hud.gd` — delayed stick claim + right-hand cluster
- `scripts/data/tunables.gd` — `ZOOM_MAX` 4.0
- `design/camera.md`, `design/tunables.md`, `design/input.md`