## 0.2.39
- Touch controls sit lower on the right and read larger
-- The action cluster is 25% bigger and stays off the middle of the play area
-- Attack is hold-to-fire only; double-tap no longer locks RT down
- Small windows keep HUD and menu text readable
-- Debug Settings has a UI text floor slider if the auto scale is still too small
- Debug Settings can force the touch overlay on desktop so the layout can be checked without a phone

Summary: Mobile layout and text scale are easier to use, and touch attack no longer latches from a double tap.

## Agent
- `ui_text.gd`, `pause_menu_view.gd` added; pause chrome split
- I2V identity lock is neck wrap only (`tools/i2v_seeds.py`, `design/art-pipeline.md`)
- Do not hand-edit `scripts/data/changelog.json` or `version.json`