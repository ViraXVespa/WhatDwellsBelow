## 0.2.11
- Game version numbers (`0.week.patch`, later `1.major.patch`)
- Per-build changelog files for agents and a generated in-game list for the current week only
- Title card shows the current build and a What's new overlay since last launch
-- First launch or wiped save shows only this build
-- Older weeks open the public changelog on Pages
- Grok Build reads this week's notes after the session handoff, not the full history
- Week pins: Web Results at session start, Build Results on the User's `0.N.0` commit

Summary: Added version stamps, a current-week What's new popup, and agent changelog rules without loading old notes every session.