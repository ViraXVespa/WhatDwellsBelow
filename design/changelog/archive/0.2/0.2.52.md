## 0.2.52
- Art pipeline: player I2V prompts now use a short green neckband and a facing-aware female Up lock.

Summary: Regen-tree cell lookup no longer crashes, and player I2V prompts dropped wrap and buckle language that was inventing extra cloth.

## Agent
- `tools/anim_review_tree.py` maps Bible cells with `dict(zip(CELL_NAMES, split_equal_3x3(...)))`. `split_equal_3x3` returns a list.
- `tools/i2v_seeds.py`: `IDENTITY_LOCK["female_up"]` omits neckband, wrap, scarf, and collar. Shared head no longer names a hip belt buckle. Walk is a short in-place step with a small arm swing. Treadmill language is “as if on an invisible treadmill” only.
