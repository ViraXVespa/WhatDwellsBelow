## 0.4.0
- Opened development week 4 on the live path
-- Grok Build Week 4 completion commit
- Placeholdia guild roofs are russet shingles; storefronts have a short striped cloth awning; the vendor stall uses a canvas tarp
- Enter dungeon shows “Consciousness slips the body…” immediately, then the floor loads under that overlay
- Dungeon floors paint nearby space first; distant props, extra crystals, ambushes, and the Floor Guardian stream in as you explore
- The large dungeon map builds when you open it, not while the floor is loading

Summary: Week 4 Grok Build snapshot. Placeholdia roofs and stall cloth are in, and entering a floor covers immediately while nearby dungeon space loads first.

## Agent
- Series-open SHA / `grok_build_w4`: wait for the User completion commit. Do not invent a hash. `grok_web_w3` stays `b87bd169fb4dce839753a37cb8dbb7a837d82f48`. Tag `archive/grok-build-w4` after they send the SHA.
- Folded unpublished `0.3.44` notes (never stamped): Grok Build does not measure or split live scripts for the 10KB cap while running; over-cap files may stay on main until a Grok Bot size sweep. Web / chat Phase 6 paste-emit sizing is unchanged.
- Enter: `present.cover_enter` on Floor Crystal press, `wait_painted` (always-timer + two `frame_post_draw`) before SFX / hub UI close / `save_now` / scene change. `release_enter` after `dungeon_boot.ready_floor` (1.05s fade). Loadout pause must not block that wait. Do not `force_draw` on the same GUI stack as the cover.
- Stream: world props, ambushes, extra crystals queue after boot and instantiate on the 0.2s pulse inside `STREAM_IN`. Floor Guardian is a stream job. Boot builds nearby geo only. P5 still eager-spawns the full prop dump. `stream_all` / `force_all` dumps enemies, queues ambushes, and flushes props.
- Lazy loads: large map on first map-view; gear UI (`progress_ui`) and `crystal_ui` on first use, not const-preloaded in `dungeon_boot`. Dead-end crystal spur checks stop at `crystal_deadend_len` (hub-cell set). Title → Play does not sync-preload plaza tiles.
- Concurrent Grok Build roles (one week pin): slice, Bot notes (`tools/bot_opt.py`), PC offload, Smoke tests.
- Catalog runners this week: `bot_opt.py`, `code_map_lib.py` / `patch_code_map.py` / `check_code_map.py`, `show_func.py`, `run_load_timing.ps1`, `run_dungeon_load_timing.ps1`, `run_dungeon_map.ps1`. Isolated stills: `tools/run_isolated_grok.py`.
- Plaza tiles: `assets/tiles/plaza_roof.png`, `plaza_tarp.png`, `plaza_awning.png`. Mesh/shader: `camp_build_mesh.gd`.
- Do not hand-edit `scripts/data/version.json` (CI stamps `0.4.0` on the User’s push). Do not emit `changelog.json`. `tools/archive_prior_changelogs.py` stays off until that stamp (`version.json` is still `0.3.43` and would park this file).
