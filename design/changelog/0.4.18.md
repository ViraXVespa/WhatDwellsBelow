## 0.4.18

- Set `WDB_AGENT_SESSION=github-actions` on the bot-gate Actions job so `check_script_cap.py` / `agent_log` can run without a Grok VM session folder.
- PR script-cap no longer fails with `agent_log: no session key`. Allowlist and real over-10KB fails are unchanged.

Summary: bot-gate CI gets a session key; cap/allowlist checks can mean what they say.
