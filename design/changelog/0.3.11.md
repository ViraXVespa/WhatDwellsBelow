# 0.3.11

## Tools / docs

- Park prior-series changelogs under `design/changelog/archive/{epoch}.{series}/` (moved all `0.2.*` notes into `archive/0.2/`).
- `tools/archive_prior_changelogs.py` / `.ps1` cleans the flat folder when series advances; CI stamp + Pages run it before `build_changelog.py`.
- Pages `/changelog/` includes flat current files and `archive/*/*.md`.

Summary: Changelog layout keeps only the current week flat; older weeks archive automatically on series bump.
