# 0.3.11

## Tools / agents

- Shared PC-offload catalog: design/pc-offload.md (Bot + Build).
- New runners: 	ools/summarize_scripts.ps1 / .py, list_facade_cluster.ps1, check_script_cap.ps1, 
un_build_gate.ps1, clean_agent_logs.ps1.
- Grok Build may write tool summaries under _logs/ (still gitignored; do not commit). Prefer reading summaries over bodies for inventory / cap / post-slice verify.
- 	ools/clean_agent_logs.ps1 + smoke orphan-phase log prune so _logs/ does not clog the disk.

## Refactor / layout

- Local folder relocate runner: 	ools/move_script_cluster.ps1 / .py (git mv facade + siblings + .uid, rewrite 
es:// / docs paths, summary at _logs/move-cluster/summary.txt). Preserves a single UTF-8 BOM on rewrite.
- Parked moves applied: debug_menu* -> scripts/debug/debug_menu/; sfx.gd -> scripts/audio/; sample facade folder gear_board* -> scripts/ui/gear_board/.
- No gameplay behavior change.

Summary: PC offload runners plus parked folder moves for debug menu, sfx, and gear_board.
