# 0.3.11

## Refactor / layout

- Local folder relocate runner: `tools/move_script_cluster.ps1` / `.py` (`git mv` facade + siblings + `.uid`, rewrite `res://` / docs paths, summary at `_logs/move-cluster/summary.txt`).
- Parked moves applied: `debug_menu*` -> `scripts/debug/debug_menu/`; `sfx.gd` -> `scripts/audio/`; sample facade folder `gear_board*` -> `scripts/ui/gear_board/`.
- No gameplay behavior change.
- Mover preserves a single UTF-8 BOM on rewrite (avoid double-BOM parse failures).
