## 0.3.12
- Bag-full floor loot shows one toast that can expire. Standing on the pile no longer pins that message or blocks later alerts.
- HP orbs stay on the ground when you are already at full health, so a later wound can still take them.

Summary: Couch-readable toasts and HP orbs that wait until you actually need them.

## Agent
- `scripts/app.gd` identical-toast hold; `scripts/world/pickup.gd` bag-full retry + full-HP orb keep.
- Smokes: P6 `hold_ok`, P5 `orb_ok`.
