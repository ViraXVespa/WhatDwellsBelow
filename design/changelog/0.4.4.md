## 0.4.4

- Add BOT.md plus Linux bot_status / check_script_cap / allowlist / bot-gate so Grok Bot does not load the web/Build protocol backpack.
- Keep reuse-map and grok-bot-opt markdown as the only queues; status reads them.
- Agent path files boot the cloud Refactorer from BOT.md and python tools/bot_status.py.
- check_load_graph treats root BOT.md like AGENTS.md so routes.yaml may name it.

Summary: Grok Bot gets a parallel onboard surface without rewriting web/Build protocols.
