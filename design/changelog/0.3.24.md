## 0.3.24

- Parked attack body stills: `design/art-attack-keyframes.md` plus `tools/attack_keyframes.py` and `tools/attack_keyframes_log.json`.
- Do not open those files unless the User resumes the attack animation keyframe pipeline. Resume command: `python tools/attack_keyframes.py --resume`.
- `tools/i2v_seeds.py` stays I2V + overlay prompts. Unarmed two-hand attack I2V still invents props; coil stills are the parked alternative.
- Authored grip is character-left-hand high. Other handedness is runtime flip plus Left/Right remap. Do not generate a second grip sheet.
- Coil sheet `coil_v22` is the last repeatable female Down stack (fists, idle feet). Next roll is `coil_v30`. Session Imagine ids are dead.

Summary: Attack keyframes are tabled for later web sessions without loading that door on walk I2V or Godot work.

## Agent

If the User names coil stills or this pipeline, open `design/art-attack-keyframes.md` and print `--resume`. Otherwise stay on `design/art-pipeline.md` / `design/art-i2v.md`.
