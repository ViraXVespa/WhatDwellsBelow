## 0.3.2
- Placeholdia load warms the player before the overlay drops
-- Status copy is "Warming things up for you..."
-- Walk/idle frames and a dummy `move_and_slide` run while the dim is fully opaque
- First stick in the yard no longer hitching on first GPU upload / first plaza collision
- Enemy pack chase no longer tanks the frame
-- Enemy list cached once per process frame
-- LOS rays use `los_period` (0.12s), staggered, skipped far away, always fresh in strike range
-- Separation stops after `sep_max` (6) neighbors
- Bow shots in the dungeon no longer occupancy-test every enemy and breakable each physics frame
-- `Cover.hit_shot` only runs on hosts near the arrow tip
-- Sprite sample points reused for the current frame
- Cream square under the 91% loader was the idle aim-line 1×1 mesh; it stays hidden until `update_line`
- Adrenaline aura stays hidden until its alpha rises
- Debug values page exposes `los_period` and `sep_max`

Summary: Load hitch, pack-chase hitch, and bow occupancy hitch are cheaper without changing cover, pierce, or aggro rules. The cream loader square is gone.
