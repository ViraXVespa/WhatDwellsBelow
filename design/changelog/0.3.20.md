## 0.3.20
- Tightened agent design docs so path rules live in one place
-- `AGENTS.md` keeps the Path table; session files keep only their own procedure
-- Dropped repeated constraint text from the coverage checklist
-- Removed a stale topic-map row that pointed at a missing file
- Changelog entries must start with `## {label}`, never `# {label}`

Summary: Agent documentation is leaner for the next Grok Build session, and changelog headings are locked to H2.

## Agent
- Revise: `AGENTS.md`, `design/protocol.md`, `design/README.md`, `design/web-session.md`, `design/grok-build.md`, `design/grok-bot-session.md`, `design/coverage.md`, `design/versioning.md`
- New: `design/changelog/0.3.20.md`
