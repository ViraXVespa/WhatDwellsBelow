## 0.4.7

- Grok Build slice boot: name an area, run start_build_slice, gather on live, User launches the FORK worktree argv off clean main.
- Week start is the week_start.ps1 script, not a new-week passphrase.
- Fork-session is the return from that gather pin after a red postcard; the guilty transcript does not keep patching.
- Drop stage_patch / promote_patch from the loop detector and the pc-offload catalog.
- read_summary uses the session path only; list_xref header matches agent_log.

Summary: Build slice boot plus harness hygiene: week_start is the week trigger, worktrees fork from a gather pin, dead patch tools stay gone.
