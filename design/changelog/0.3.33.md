## 0.3.33

- Agent navigation now lives in `design/routes.yaml`, not in `See also:` fields.
- Topic bodies may not name other `design/*.md` files; doors keep those paths only in the Job table.
- `tools/check_load_graph.py` reads the yaml table and fails on leftover See also / stray cites.
- `tools/apply_doc_routes.py` stripped See also lines and rewrote topic cites to job phrases.
- Load-graph check is a PC-offload catalog runner.

Summary: Agent doc routing is a declared yaml graph. No player-facing game change.
