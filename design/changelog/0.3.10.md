## 0.3.10
- Split oversized forge UI script into facade + helpers so live scripts stay under the size cap.
-- `gear_board_anvil_forge.gd` remains the public entry; edit / pick / job helpers hold moved code only.

Summary: Size-split the anvil forge UI script with no gameplay change.
