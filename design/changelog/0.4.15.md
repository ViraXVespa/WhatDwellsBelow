## 0.4.15

- Docs facade: `design/gear-ui.md` is the door under 4KB.
- Siblings: `gear-ui-board.md`, `gear-ui-stats.md`, `gear-ui-reequip.md`, `gear-ui-chrome.md` (each under 8KB).
- Job table covers former gear-ui `##` clusters; callers keep linking the facade.
- `design/routes.yaml` registers `gear_ui.board|stats|reequip|chrome`. No binding change.
- `tools/bot_status.py` `_git` uses `rstrip` so porcelain leading spaces survive (allowlist path parse).

Summary: Gear-ui door + four siblings; facade path unchanged.
