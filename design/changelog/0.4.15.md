## 0.4.15

- Docs facade: `design/gear-ui.md` is the door; moved **Re-equip and Anvil submenus** into `design/gear-ui-reequip.md`.
- Job table covers former gear-ui `##` clusters; callers keep linking the facade.
- `design/routes.yaml` registers `gear_ui.reequip`. No binding change.
- `tools/bot_status.py` `_git` uses `rstrip` so porcelain leading spaces survive (allowlist path parse).

Summary: Gear-ui door + reequip sibling; facade path unchanged.
