## 0.2.34
- Player I2V prompts cover every Animation Browser Regenerate flag
-- Per-class beat sheets: `attack_great_axe`, `attack_staff`, `attack_longbow`, matching `special_*`, `gather_pickaxe`, `gather_hatchet`, `death`, `dispel`
-- Loop wrapper for walk; one-shot wrapper for the rest; unknown actions no longer become walk
-- `gather` writes both tool prompts
- “Dispel” I2V is seppuku with a small knife; death and Dispel do not paint blood
- `tools/pack_oneshot.py` packs accepted one-shot clips into engine names
-- Attack / special / gather default to 6 frames; death and Dispel keep clip length
- Animation Browser lists `gather_pickaxe` / `gather_hatchet` and falls back to `gather_*`
- `idle_to_walk` / `walk_to_idle` stay pack-only

Summary: Regen flags now emit the matching player I2V prompt, and accepted one-shot clips can be packed for the Animation Browser.

## Agent
- Overlay / I2I prompts and enemy I2V were out of scope
- Engine blood pools under death / Dispel are not shipped
- Revert this slice: `tools/i2v_seeds.py`, `tools/anim_review_lib.py`, `tools/pack_oneshot.py`, `scripts/debug/anim_scan.gd`, plus the design files touched in this goal