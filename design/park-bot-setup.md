# Park: Bot setup (The Refactorer)

Status: parked
Read when: resume parked bot_setup, Refactorer profile, cloud clone, pool definitions


## Goal

Rework Grok Bot (the product teammate named The Refactorer) so Jr. maintenance work is cheap and continuous without a fat backpack. This park is the mandate.


## Role

Jr. programmer. Punch list only: size, extract, reuse-map Brief, relocate, doc facades, named opt items, catalog/tooling.

Not: new player-facing systems, tunables, combat feel, playtest AI, art/I2V, pause redesign.

A new helper file or 10KB split is not a new game system.


## Machine

Work on the Bot cloud clone and GitHub PR. Not the User PC path (not WDB_ROOT, not the Steam Godot box).

All Bots on the account share one cloud computer. Do not treat two Bots as disk isolation.

Hard reset: delete the Bot and recreate with this profile. That does not wipe the shared VM; clean or reclone the tree.


## Profile (thin)

Ten lines, not AGENTS.md. Job title, one flow at a time, 10KB cap, no new player-facing systems, check queues and git not memory, after a cluster drop those file bodies.

Skills via / for a named job. Do not paste the repo on onboard. First message is the job or list, not the tree.


## PR

One open Bot PR. Keep committing until the User merges or quota ends. Then a new PR.

Not one PR per opt id. Commit per cluster on that branch. User squash-merges when they choose.


## Pools

Reuse-map (design/reuse-map.md): one Brief of same-shape live UI extracts. Implement the whole Brief. Do not invent rows. Current staged Brief is UI chrome (wipe, theme tokens, confirm_dlg not press-twice, ui_open/footer, focus wire, skill-row helper, PromptView footers). Pause skills and recap are not one screen.

Opt queue (design/grok-bot-opt.md): standing ids. Tools, boot, timing, preload. opt-002 is facade preload literals on Autoload/flow only, not preload(variable), not tree-wide Res.*.

Size / relocate / docs: Job-table rows. Not a third queue file.


## Size week in one chat

The User has one chat, no thread control. One kickoff: work the oversize list top-down. Do not ask permission per file. After each cluster, stop holding those bodies. Report when the list is empty or actually blocked.

Grok Bot has no per-task token ledger. Settings show weekly usage. Slim profile plus one-list kickoff is the lever.


## Not this teammate

Playtest Bot with Godot on the cloud box, vision loops, locale tr() sweep. Separate parks or later items.
