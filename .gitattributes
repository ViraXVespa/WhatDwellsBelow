# Normalize EOL for all files that Git considers text files.
* text=auto eol=lf

# Keep Godot / web binaries untouched (Pages wasm/pck, sprites).
*.wasm binary
*.pck binary
*.png binary
*.jpg binary
*.ico binary
*.zip binary

