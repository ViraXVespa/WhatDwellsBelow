## 0.2.15
- Web / chat Phase 4 now loads a revise file from the page-fetch artifact plus a GitHub `size` check
-- The tool card is a summary, not the file
-- One fetch per path; do not retry or invent a second pipeline
- Asking the User to paste a live file is last resort only
- Phase 3 lists emit paths (`new` / `revise` / `delete`) and does not request pastes

Summary: Agent protocol for web sessions: read the saved fetch artifact and verify bytes before emitting; paste only if that check fails.

## Agent
- `design/web-session.md`
- `AGENTS.md`