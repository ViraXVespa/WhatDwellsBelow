## 0.3.37

- Strip cross-role design-doc path cites so the load-graph role allowlist and cycle walk pass.
- Give each routed file one routes.yaml role; stop topic skip-door phrases; drop See also from the doc-split recipe.
- Keep sessions, session-log, and reuse-map as notes_exempt only so leave-off / log / staging briefs stay outside the gate cycle walk.
- Mark attack-keyframes as a parked job and give input/inventory live-snapshot jobs unique routes.yaml keys.
- Give each topic door a disjoint read_when phrase so one task matches one facade.

Summary: Load-graph checker now enforces role allowlists, citation cycles, skip-door stems, parked jobs, unique job keys, and disjoint read_when phrases. No player-facing game change.
