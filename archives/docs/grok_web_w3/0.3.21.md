## 0.3.21
- Agent reuse map so extract / DRY / shared-helper work has one index
-- No player-facing gameplay change in this slice
- Grok Bot opens that map only when asked for reuse work

Summary: This build documents where copied live code should land. Play is unchanged.

## Agent
- New `design/reuse-map.md` (owners, BOT-01–10, kits, do-not-merge, PR order).
- Pointers: `AGENTS.md`, `design/README.md`, `design/grok-bot-session.md`, `design/refactor.md`.
- Plate tokens unify to SplitMenu when that work runs: dim `Color(0.03, 0.02, 0.02, 0.82)`, plate `Color(0.13, 0.10, 0.08, 0.97)`, edge `Color(0.55, 0.42, 0.22, 1)` h=8.
- `{label}` = baked `0.3.20` patch + 1. Do not hand-edit `version.json` or `changelog.json`.
