# 0.3.12

## Tools / agents

- Grok Bot may refactor topic design docs into art-pipeline-style **doors + siblings** (`design/doc-refactor.md`) to cut routing tokens without changing binding meaning.
- Pilot splits: `ui.md`, `debug.md`, `input.md`, `inventory.md` are doors; heavy sections live in siblings (`ui-*.md`, `debug-*.md`, `input-*.md`, `inventory-*.md`).
- `tools/list_oversize_docs.ps1` inventories fat topic markdown (read `_logs/oversize-docs/summary.txt` only).
- Clarify that post-merge `chore: stamp` commits are CI (`version.yml`) only; Bot must not offer or create them (`design/grok-bot-session.md`).
- Windows agent shell: `tools/write_utf8_file.py` + `tools/run_agent_py.ps1` (ephemeral scripts under `_logs/agent-py/` auto-deleted after run); forbid `python -c` / PS-expanded doc bodies (`design/pc-offload.md`).
- Bag-full floor loot shows one toast that can expire. Standing on the pile no longer pins that message or blocks later alerts.
- HP orbs stay on the ground when you are already at full health, so a later wound can still take them.

Summary: Documentation facades join script facades in the Bot mandate; four largest topics now route through job tables, and item toast/orb behavior stays readable and queued correctly.

## Agent
- `scripts/app.gd` identical-toast hold; `scripts/world/pickup.gd` bag-full retry + full-HP orb keep.
- Smokes: P6 `hold_ok`, P5 `orb_ok`.
