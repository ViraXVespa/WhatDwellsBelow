## 0.3.15
- Title → Play no longer hangs for seconds on the first stick in Placeholdia
-- The loader stays a near-black sheet (`#000000` at alpha `0xFE`) while the square draws underneath
-- Walk, start, and stop frames for the selected body are preloaded and drawn under that sheet before control returns
- Web export bakes mipmaps for world sprites, tiles, props, and FX before packing
-- Runtime filter apply skips rebuilding a chain that already exists
- Extract-wake is unchanged

Summary: Hub movement after Play stays smooth on GitHub Pages. First-use shader and texture work happens behind the loader, and mip chains ship in the PCK instead of generating on the player’s machine.

## Agent
- Source: `scripts/app_flow.gd`, `scripts/world/player_anim.gd`, `scripts/world/camp.gd`, `scripts/world/sprite_filter.gd`, `scripts/ui/loader.gd`, `tools/enable_texture_mips.py`, `tools/export_web.ps1`
- Docs: `design/web-session.md`, `design/protocol.md`, `design/hub.md`, `design/audio-visual.md`, `design/player.md`, `design/save-tech.md`, `design/README.md`, `design/pc-offload.md`
