## 0.3.30

- Split the live code map out of `design/README.md` into `design/code-map.md`. The topic index is one row; agents open one system row before walking scripts.
- Tightened agent load rules: `See also` is not a read list. Path files no longer reboot `AGENTS.md`. Mid-week slices skip `design/changelog/`.
- Grok Bot flows start at the already-open door plus one recipe plus one code-map row. They do not reopen protocol or constraints.
- Topic doors keep one boundary `See also` or none. Job-table siblings do not bounce back to the parent door.
- Binding game rules did not change. Numbers, forge law, Bible printer, and archive pins stay on their owner files.

Summary: Agent documentation routing was compacted so a session loads fewer design files without changing player-facing behavior.

## Agent

- New file: `design/code-map.md` (must be committed; table left `design/README.md`).
- Dead path `design/handoff-anvil.md` stays unpublished. Do not restore links to it.
- `design/reuse-map.md` remains the staged brief, not an owners encyclopedia.
- Reverts of Fast body dumps already landed in this slice (`skills.md` kill-XP / CL table, `gear-ui.md` / `hub.md` forge inlines, `overview.md` in-scope dump, `art-pipeline.md` Appendix C printer, `combat.md` pointers, `constraints.md` run-only + short checklist, `coverage.md` core rules, `save-tech.md` pointers, `tunables.md` pointers, `archives.md` “rows above”).
- Do not hand-edit `scripts/data/version.json` or `scripts/data/changelog.json`. CI stamps `0.3.30` when this file lands on `main`.
