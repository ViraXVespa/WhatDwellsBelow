## 0.3.23

- `See also:` is an index, not a read list. Protocol files this slice touches say so in the header.
- `design/reuse-map.md` is a staging brief web / chat writes in Phase 7. It is not an owners table or a standing BOT list. An empty **Brief** means Bot stops.
- Grok Bot treats a non-empty reuse-map file as one PR. Flow: `design/grok-bot-reuse.md`. Bot does not mark the brief done.
- `design/grok-bot-session.md` is a door. Flows: `design/grok-bot-size.md`, `design/grok-bot-extract.md`, `design/grok-bot-reuse.md`, `design/grok-bot-relocate.md`, `design/grok-bot-docs.md`. One Job-table sibling per session.
- Docs-only web sessions skip Phase 4–6 after Phase 3.
- Durable do-not-merge rows live in `design/grok-bot-extract.md`. Refactor recipe no longer points at reuse-map for owners or kits.

Summary: Agent routing now opens fewer files by default; reuse work is a web-written brief Bot runs as a single PR.

## Agent

Door first: `design/grok-bot-session.md`. Do not load `design/reuse-map.md` on a size sweep. Revert this slice by restoring the previous protocol files and the old reuse-map worklist if a Bot session still needs that completed extract brief.
