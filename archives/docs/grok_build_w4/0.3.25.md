## 0.3.25
- Isolated Imagine / I2V jobs default to a scratch Grok Build session outside the repo so media turns do not reload `AGENTS.md`
- Shared runner `tools/run_isolated_grok.py` stages style sheets, writes a kind-specific job, inspects the cwd, then runs `grok -p`
- Kind routing: tile, character, i2v, ui, still — one bundled game-asset specialist each
- Repo skills `imagine-isolated` and `i2v-isolated` tell the parent session to spawn that runner instead of generating in-place
- In-session Imagine stays allowed only for a short exception list (User said so, already in scratch, edit of an attached image, runner cannot start)

Summary: CLI media generation can run in a thin temp directory so stills and clips do not pay the full game-repo context on every turn.

## Agent
- Binding: `design/isolated-media.md`
- Skills: `.grok/skills/imagine-isolated/SKILL.md`, `.grok/skills/i2v-isolated/SKILL.md`
- First live fixture when quota resets: `--kind tile --bible-style` for a seamless Placeholdia roof; do not change camp UV crop in this slice
- Web session wrote files for paste; Grok Build on disk should land the same tree
- Next patch label after bake: `0.3.25` (current `version.json` is `0.3.24`)
