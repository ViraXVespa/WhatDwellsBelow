## 0.3.29

- Tightened agent load routing so topic `See also` fields no longer form read cycles.
- `design/sessions.md` is leave-off only; it no longer boots a fresh Grok Build instance.
- `design/load-graph.md` is User-named routing work only.
- Live code map no longer lists design-doc rows (reuse-map / isolated-media).
- Added `tools/check_load_graph.py` to verify the routing rules.

Summary: Agent design-doc routing no longer loops back on itself.
