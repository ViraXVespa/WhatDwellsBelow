## 0.3.17

- Title → Play holds the loader sheet while Placeholdia’s full yard (including the grass pad and the food stall) is drawn, then puts the camera back to the zoom you set
- Web export bakes world-texture mipmaps during the Pages build so first walks hitch less on a new build

Summary: First load into the hub should hitch less, especially zoomed in and walking toward the vendor, without changing your saved camera zoom.

## Agent

- Live: `scripts/world/camera_rig.gd`, `scripts/world/camp_warm.gd`, `scripts/world/camp.gd`, `scripts/app_flow.gd`, `.github/workflows/pages.yml`
- Docs: `design/hub.md`, `design/save-tech.md`, `design/audio-visual.md`, `design/camera.md`, `design/README.md`
- Do not commit `mipmaps/generate=true` on tracked `.import` files. `tools/enable_texture_mips.py` runs in the export workspace only (Pages + `export_web.ps1`). Script is idempotent.
- Extract-wake still skips Title → Play warmup.
