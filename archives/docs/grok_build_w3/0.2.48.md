## 0.2.48
- Escape in browser fullscreen opens the pause menu instead of leaving fullscreen
-- Chromium / Edge. Hold Escape to leave fullscreen if the browser requires it
- F1 also opens pause on the web, including Safari and Firefox, without leaving fullscreen
- Pause → System and Alt+Enter still toggle fullscreen

Summary: Capture Escape where the browser allows it, and add F1 as a pause bind that every browser can keep inside fullscreen.
